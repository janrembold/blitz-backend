{
  "name": "migrate",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "db-migrate": "0.11.12",
    "db-migrate-pg": "1.2.2"
  },
  "scripts": {
    "migration.create": "db-migrate create",
    "migration.up": "echo \"Migrate $DB_NAME on $DB_HOST:$DB_PORT with ARN: $SECRET_ARN\" && db-migrate up"
  }
}